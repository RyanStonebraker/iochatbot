$(document).ready(function () {
  // function drawLine(start, end, name="") {
  //   var length = Math.sqrt(Math.pow((start.x - end.x), 2) + Math.pow((start.y - end.y), 2));
  //   var angle = Math.asin((start.y - end.y)/length) * 180/Math.PI;
  //
  //   let line = "<div class='line " + name + "'style='height: "
  //            + length + "px;top: " + start.y
  //            + "px;left:" + start.x
  //            + "px;transform:rotate(" + angle + "deg);'></div>";
  //
  //   $("section.stage").append(line);
  // }

  
});
